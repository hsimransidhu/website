'use strict';

//utility functions 
function select(selector) {
    return document.querySelector(selector)
}
let projectName = 'Super Pizza';
let address = 'henlow bay';
 function printMenu() {
    // fetch the menu
    //forEach..

 }
